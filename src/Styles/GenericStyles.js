import { Dimensions, StyleSheet } from "react-native";

export default genericStyles = StyleSheet.create({
  //the two most used colors
});

export const greyColor = "#A4AAB7",
  blackTheme = "#171C26",
  brightGrey = "#A4AAB7";
